import {makeStyles} from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
    MemberInput: {
        border: '1px solid #ced4da',
        borderRadius: '3px',
        padding: '0 .7rem'
    }
}));

export {useStyles};