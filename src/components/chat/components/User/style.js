import {makeStyles} from '@material-ui/core/styles';

const useStyles = makeStyles((theme) => ({
    badge: {
        background: '#4bbf73',
        color: 'white',
        borderRadius: '3px'
    },
    MemberItem: {
        margin: '0.4rem 0'
    },
}));

export {useStyles};